# --- Database ---
PGHOST=10.98.68.254
PGPORT=5432
PGUSER=rpa_planning_user
PGPASSWORD="<replace-with-database-password>"
PGDATABASE=rpa_planning

# --- Server ---
PORT=6000
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=8h
CORS_ORIGIN=http://localhost:3000

# --- Default Superadmin (created on first run if no users exist) ---
TENANTADMIN_USERNAME=tenantadmin
TENANTADMIN_PASSWORD=<replace-before-first-start>
SUPERADMIN_USERNAME=superadmin
SUPERADMIN_PASSWORD=<replace-before-first-start>
